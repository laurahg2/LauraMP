# Add Two Numbers

# 1. Create a new Python file
# 2. Copy and Paste the function from the slides:



def add_two_numbers(a, b):
   return a + b


result = add_two_numbers(1,1)
test = add_two_numbers(9, )
assert result == 2
assert test == -10
assert


# 3. Write a number of unit-tests to test the following scenarios:
#    - `adds two whole numbers`
#    - `adds a positive whole number to a negative whole number`
#    - `adds two floating point numbers`
#    - `adds a string to a whole number`
#    - `adds two strings`

# _note: you may find you need to improve your function to cater to all scenarios listed_